{
  "name": "prettier-plugin-elm",
  "version": "0.4.1",
  "main": "src",
  "repository": "https://github.com/gicentre/prettier-plugin-elm",
  "description": "Adds Elm language support to Prettier via elm-format",
  "license": "BSD-3-Clause",
  "files": [
    "src"
  ],
  "keywords": [
    "elm",
    "elm-format",
    "gicentre",
    "prettier",
    "prettier-plugin"
  ],
  "scripts": {
    "clean:coverage": "rimraf coverage",
    "clean:test": "rimraf cache",
    "lint": "prettier --check {*,src/**/*,test/*}.{md,js}",
    "test": "jest -c jest.test.config.js --no-coverage",
    "test:coverage": "jest -c jest.test.config.js",
    "format": "prettier --write {*,src/**,test/**}.{md,js}",
    "prepublishOnly": "yarn lint && yarn test"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "prettier": "prettier/prettier#439b1649ff9298b56b4f5e68214de6c2b29c6f26",
    "rimraf": "^2.6.2",
    "sleep-promise": "^8.0.1"
  },
  "dependencies": {
    "elm-format": "^0.8.1",
    "execa": "^1.0.0",
    "make-dir": "^1.3.0",
    "object-hash": "^1.3.0",
    "serialize-error": "^3.0.0",
    "temp-dir": "^1.0.0"
  },
  "prettier": {},
  "husky": {
    "hooks": {
      "pre-push": "yarn lint"
    }
  }
}
